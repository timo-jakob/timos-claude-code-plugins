{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "description": "Renovate config — primary job is keeping the GitHub Action pins inside the bootstrap workflow *templates* current (issues #109, #541). Renovate's stock github-actions manager already watches .github/workflows/ paths (including the plain-.yml templates); the customManager below extends that coverage to *.tmpl files under development/skills/bootstrap/templates/ where the action pins are baked into the workflow source we ship to downstream repos.",
  "timezone": "Europe/Berlin",
  "schedule": ["before 6am on monday"],
  "dependencyDashboard": true,
  "dependencyDashboardTitle": "Renovate dependency dashboard",
  "prHourlyLimit": 5,
  "prConcurrentLimit": 5,
  "labels": ["dependencies"],
  "customManagers": [
    {
      "customType": "regex",
      "description": "Extract GitHub Action `uses:` refs from bootstrap workflow templates (*.tmpl). Since #541 every template ref is pinned to a full commit SHA with a `# <tag>` comment (the shipped semgrep gate blocks mutable tags in downstream repos), so the regex captures the SHA as currentDigest and the tag comment as currentValue — Renovate keeps the digest fresh and bumps the tag comment alongside.",
      "managerFilePatterns": ["/\\.tmpl$/"],
      "matchStrings": [
        "uses:\\s+(?<depName>[a-zA-Z0-9_.-]+/[a-zA-Z0-9_.-]+)(?:/[a-zA-Z0-9_./-]+)?@(?<currentDigest>[a-f0-9]{40})\\s+#\\s+(?<currentValue>[a-zA-Z0-9._-]+)"
      ],
      "datasourceTemplate": "github-tags"
    }
  ],
  "packageRules": [
    {
      "description": "Batch every GitHub Action upgrade (both .yml and .tmpl) into one weekly PR so the user has a single review + merge for the whole action surface.",
      "matchManagers": ["github-actions", "custom.regex"],
      "groupName": "github-actions",
      "groupSlug": "github-actions",
      "commitMessageTopic": "GitHub Actions"
    }
  ]
}
