# Use the web UI

How to work with {{PROJECT_NAME}} in the browser.

!!! note "Seeded page"
    This page was seeded because the project exposes a web UI. Grow it
    alongside the features: each user-visible UI change should update this
    guide (or add a sibling how-to page) in the same PR.

## Open the UI

Document where the UI is served and how to sign in.

## Common tasks

Document each recurring job as its own section — the screen to open, the
steps to take, and what the reader should see when it worked.
