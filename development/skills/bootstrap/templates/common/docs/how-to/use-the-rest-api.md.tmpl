# Use the REST API

How to call {{PROJECT_NAME}} over HTTP.

!!! note "Seeded page"
    This page was seeded because the project exposes a REST interface. Grow
    it alongside the features: each user-visible endpoint change should
    update this guide (or add a sibling how-to page) in the same PR.

## Base URL and authentication

Document where the API lives and how a caller authenticates.

## Make a request

Show a complete request/response pair for the most common call:

```console
$ curl -s https://<host>/<endpoint>
```

## Endpoints

Document each endpoint's job here as it ships; put the full request/response
schemas in [Reference](../reference/index.md).
