# Architecture

The architecture home for {{PROJECT_NAME}}. C4 architecture documentation
(Mermaid `C4Context` / `C4Container` diagrams, maintained alongside the code)
will live here — the machinery that seeds and maintains it ships separately;
until then this page is a deliberate placeholder.

In the meantime, structural decisions worth recording can live on this page
or in [Explanation](../explanation/index.md).
