{
  "_comment": "Notable, user-relevant changes to bootstrap templates, so the template-drift detector can name WHAT a re-bootstrap would deliver (#400) instead of only saying a file drifted. Keyed by template path exactly as the rendered file's `# claude-bootstrap: rendered from <path>` marker records it (relative to skills/bootstrap/templates/). Each entry: `version` = the development plugin version the change shipped in; `issue` = the GitHub issue; `blocking` = true when it changes required-check / merge-gating behavior (the detector escalates these); `summary` = one line a human can act on. Add an entry whenever a template change is worth surfacing on drift; omit cosmetic ones. The detector lists entries whose version is newer than the rendered file's marker version.",
  "public/.github/workflows/quality-public.yml.tmpl": [
    {
      "version": "1.49.2",
      "issue": 386,
      "blocking": true,
      "summary": "image scan is now path-conditional — it gates only PRs that touch container files (Dockerfile/.dockerignore/compose/.snyk), so app & dependency PRs are no longer blocked by pre-existing base-image CVEs they can't influence"
    }
  ],
  "private/.github/workflows/quality-private.yml.tmpl": [
    {
      "version": "1.49.2",
      "issue": 386,
      "blocking": true,
      "summary": "image scan is now path-conditional — it gates only PRs that touch container files (Dockerfile/.dockerignore/compose/trivy.yaml), so app & dependency PRs are no longer blocked by pre-existing base-image CVEs they can't influence"
    }
  ],
  "common/.pre-commit-config.yaml.tmpl": [
    {
      "version": "1.58.4",
      "issue": 518,
      "blocking": false,
      "summary": "the Python coverage-floor pre-push hook now withholds its verdict when coverage.xml was recorded against a different tree (shared-venv editable install in a git worktree) and names the per-worktree-venv remedy, instead of false-failing changed lines as 0% covered"
    }
  ],
  "common/.github/workflows/spec-publish.yml.tmpl": [
    {
      "version": "1.126.0",
      "issue": 706,
      "blocking": false,
      "summary": "the spec-publish workflow gained the optional APIM governance channel: two presence-gated steps (in the same publish job) that publish the same contracts/vN/openapi.yaml spec + docs to a portal and deploy the apim/ proxy config-as-code — active only when an apim/ directory exists, with org-replaceable reference commands"
    }
  ]
}
