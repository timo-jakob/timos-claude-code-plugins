---
name: demo
description: A demo skill in the test fixture. Valid frontmatter, non-empty body.
---

This is the demo skill body. It exists so the skill_validator sees a non-empty
body and a name that matches the directory.
